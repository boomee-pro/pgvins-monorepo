version: '3.8'
services:
  redis:
    image: redis:latest
    command: [ "redis-server", "--bind", "redis", "--port", "6379" ]
  rcli:
    image: redis:latest
    links:
      - redis
    command: redis-cli -h redis
